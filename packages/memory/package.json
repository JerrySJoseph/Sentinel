{
  "name": "@sentinel/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Persistence layer for sessions, messages, and tool runs (Postgres via Prisma)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prisma:generate": "XDG_CACHE_HOME=.cache prisma generate",
    "prisma:migrate:deploy": "XDG_CACHE_HOME=.cache prisma migrate deploy",
    "prisma:migrate:reset": "XDG_CACHE_HOME=.cache prisma migrate reset --force --skip-seed",
    "build": "tsc",
    "prebuild": "pnpm prisma:generate",
    "pretest": "pnpm prisma:generate",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "lint:fix": "eslint \"{src,test}/**/*.ts\" --fix",
    "pretypecheck": "pnpm prisma:generate",
    "typecheck": "tsc --noEmit",
    "test:integration": "docker compose -f ../../infra/compose.yml --profile test up -d postgres_test && DATABASE_URL=${TEST_DATABASE_URL:-postgresql://sentinel:sentinel@localhost:5433/sentinel_test?schema=public} pnpm prisma:migrate:deploy && DATABASE_URL=${TEST_DATABASE_URL:-postgresql://sentinel:sentinel@localhost:5433/sentinel_test?schema=public} jest --config jest.integration.config.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.5",
    "jest": "^29.7.0",
    "prisma": "^6.19.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}